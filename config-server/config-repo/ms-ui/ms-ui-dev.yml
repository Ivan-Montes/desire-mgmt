
management:
  endpoints:
    access:
      default: unrestricted
    web:
      exposure:
        include: "*" 
  endpoint:
    health:
      show-details: always  
   
info:
  app:
    profile: dev
    